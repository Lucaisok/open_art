# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env
.env.*
!.env.example

# Data — own nested local-only git repo (data/.git), so individual files
# under it can never be tracked from here regardless of ignore rules; the
# published processed CSV is copied out to dataset/ instead (see
# scripts/export_dataset.py)
data/

# Working build log — kept local-only for now: names the real external
# sites/listing pages this project scrapes, unlike ANNOTATION_GUIDELINES.md
# (published — taxonomy/methodology only, no source-identifying specifics
# beyond what dataset/opportunities.csv already publishes)
workflow.MD

ANNOTATION_GUIDELINES.md